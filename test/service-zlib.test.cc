#include "common_dep.h"
#include "gtest/gtest.h"
#include "zlib.h"

TEST(ServiceZlib, Compress)
{
    // 压缩多个文件到指定的压缩包
    ASSERT_EQ(1,1);
}

// int main(int argc, char **argv)
// {
//     printf("Running main() from %s\n", __FILE__);
//     testing::InitGoogleTest(&argc, argv);
//     return RUN_ALL_TESTS();
// }
